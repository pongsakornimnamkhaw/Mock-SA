DB_HOST=localhost
DB_USER=admin_T01SA
DB_PASSWORD=T01SA
DB_NAME=backend_T01
DB_PORT=5432
DB_SSLMODE=disable
PORT=8080

# Base URL the frontend is served from (used in links sent via email, e.g. password reset)
APP_BASE_URL=http://localhost:5173

# SMTP settings for sending emails. Leave SMTP_HOST empty to log emails to the console instead.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
